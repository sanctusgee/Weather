
#: import ListItemButton kivy.uix.listview.ListItemButton
#: import ListAdapter kivy.adapters.listadapter.ListAdapter

WeatherRoot:

<WeatherRoot>
    AddLocationForm:
        orientation: "vertical"
        search_input: search_box
        search_results: search_results_list
        current_location: curr_loc
        search_button: search_btn

    BoxLayout:
        height: "40dp"
        size_hint_y: None
        TextInput:
            id: search_box
            multiline: False
            size_hint_x: 50
            on_text_validate: root.search_location()    # enter key pressed
        Button:
            id: search_btn
            text: "Search"
            size_hint_x: 25
            on_press: root.search_location()
        Button:
            id: curr_loc
            text: "Current Location"
            size_hint_x: 25
            on_press: root.search_location()
    ListView:
        id: search_results_list
        adapter:
            ListAdapter(data=[], cls=ListItemButton)
        #item_strings: []


 #   WeatherRoot:

  #  <WeatherRoot>:
  #      AddLocationForm